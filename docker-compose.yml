# ===========================================================================
# We use 1 Dockerfile and 1 docker-compose.yml file to build and 
# run the Django application in a containerized environment.
# Currently only configured to work across droplets. Will not work correctly fully local.
# ===========================================================================


#==========================
# UI + DB containers:
#==========================

services:

  portainer: # only run on dsoc droplet
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: always
    security_opt:
      - no-new-privileges:true
    ports:
      # - "9000:9000"
      # - "9443:9443"
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data


  portainer_agent: # only run on remote machines: gbt and vlba
    image: portainer/agent:latest
    container_name: portainer_agent
    security_opt:
      - no-new-privileges:true
    # environment:
    #   # Optional: Restrict agent access to your Portainer Server IP for security
    #   - AGENT_SECRET=your_optional_secret_key
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/lib/docker/volumes:/var/lib/docker/volumes
    ports:
      - "9001:9001"
    restart: always


# DSOC droplet only traefik container
  traefik:
    image: traefik:v3.7
    container_name: traefik
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

    networks:
     # Connect to the 'traefik_proxy' overlay network for inter-container communication across nodes
      # - proxy
      proxy:
        aliases:
          - traefik-metrics # using an alias here so prometheus won't care if local or droplet traefik container is running

    ports:
      - "80:80" # website HTTP
      - "443:443" # website HTTPS
      - "127.0.0.1:8080:8080" # Dashboard
      - "5432:5432" # postgres
      - "9092:9092" # kafka broker
      - "4004:4004" # etransfer command port
      - "8008:8008/udp" # etransfer data port

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./letsencrypt:/letsencrypt

    command:
      # EntryPoints
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.postgres.address=:5432"
      - "--entrypoints.kafka.address=:9092"
      - "--entrypoints.etransfer-command.address=:4004"
      - "--entrypoints.etransfer-data.address=:8008/udp"

      # Dedicated Prometheus metrics endpoint
      - "--entrypoints.metrics.address=:8082"

      # Optional redirect HTTP to HTTPS
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure" 
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--entrypoints.web.http.redirections.entrypoint.permanent=true"

      # Docker providers 
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=proxy"

      # Let's Encrypt / ACME
      # - "--certificatesresolvers.le.acme.email=${COHORT_EMAIL}"
      - "--certificatesresolvers.le.acme.storage=/letsencrypt/acme.json" 
      - "--certificatesresolvers.le.acme.httpchallenge=true"
      - "--certificatesresolvers.le.acme.httpchallenge.entrypoint=web"
      
      # API & Dashboard 
      - "--api.dashboard=true"
      - "--api.insecure=true"

      # Observability 
      - "--log.level=INFO"
      - "--accesslog=true"
      - "--metrics.prometheus=true"

      # Prometheus
      - "--metrics.prometheus=true"
      - "--metrics.prometheus.entrypoint=metrics"
      - "--metrics.prometheus.addrouterslabels=true"

  # Traefik Dynamic configuration via Docker labels
    labels:
      # Enable self‑routing
      - "traefik.enable=true"

      # Dashboard router
      - "traefik.http.routers.dashboard.rule=Host(`dashboard.docker.localhost`)"
      - "traefik.http.routers.dashboard.entrypoints=web"
      - "traefik.http.routers.dashboard.service=api@internal"

# Local dev traefik container
  traefik_http:
    image: traefik:v3.7
    container_name: traefik_http
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

    networks:
      # Local bridge network used by Traefik to reach
      # the Django container on the DSOC Droplet.
      # - proxy
      proxy:
        aliases:
          - traefik-metrics # using an alias here so prometheus won't care if local or droplet traefik container is running

    ports:
      - "80:80" # website HTTP
      - "127.0.0.1:8080:8080" # dashboard
      - "5432:5432" # postgres
      # - "9092:9092" # kafka broker
      - "4004:4004" # etransfer command port
      - "8008:8008/udp" # etransfer data port

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

    command:
      # EntryPoints
      - "--entrypoints.web.address=:80"
      - "--entrypoints.postgres.address=:5432"
      # - "--entrypoints.kafka-1.address=:9092"
      - "--entrypoints.etransfer-command.address=:4004"
      - "--entrypoints.etransfer-data.address=:8008/udp"

      # Dedicated Prometheus metrics endpoint
      - "--entrypoints.metrics.address=:8082"

      # Providers 
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=proxy"

      # API & Dashboard 
      - "--api.dashboard=true"
      - "--api.insecure=true"

      # Observability 
      - "--log.level=INFO"
      - "--accesslog=true"

      # Prometheus
      - "--metrics.prometheus=true"
      - "--metrics.prometheus.entrypoint=metrics"
      - "--metrics.prometheus.addrouterslabels=true"

  # Traefik Dynamic configuration via Docker labels
    labels:
      # Enable self‑routing
      - "traefik.enable=true"

      # Dashboard router
      - "traefik.http.routers.dashboard.rule=Host(`dashboard.docker.localhost`)"
      - "traefik.http.routers.dashboard.entrypoints=web"
      - "traefik.http.routers.dashboard.service=api@internal"


  prometheus:
    image: prom/prometheus:latest # I feel like we should pin a specific version? Rather than the latest?
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    networks:
      - proxy
      - ngradar_website_network # Giving prometheus access to both networks for metrics monitoring

    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=15d"


  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - proxy
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus


  postgres_exporter:
    image: prometheuscommunity/postgres-exporter:latest # we should have a pinned version eventually, I feel
    container_name: postgres_exporter
    restart: unless-stopped
    environment:
      DATA_SOURCE_NAME: ${DATABASE_URL}
    networks:
      - ngradar_website_network
    depends_on:
      postgres:
        condition: service_healthy



  ngradar_website:
    container_name: ngradar_website
    build:
      context: .
      target: app
    env_file:
      - .env
    depends_on:
      postgres:
          condition: service_healthy
    volumes:
      - .:/service
    # ports:
    #   - 8000:8000
    networks:
      - ngradar_website_network
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      # Tells Traefik to route public requests matching your domain/IP to this container
      - "traefik.http.routers.ngradar_website.rule=Host(`${WEBSITE_DOMAIN_NAME}`)"
      - "traefik.http.routers.ngradar_website.entrypoints=${HTTP_ENTRYPOINT:-web}" 
      - "traefik.http.routers.ngradar_website.service=ngradar_website"
      - "traefik.http.routers.ngradar_website.tls=${TLS:-false}" #false when local, true on droplet
      - "${CERTRESOLVER}" # reads dummy label when local, and certificate resolver when deployed on droplets with HTTPS

      # Optional: Explicitly tell Traefik which port your web app container listens on internally
      - "traefik.http.services.ngradar_website.loadbalancer.server.port=8000"
    stdin_open: true
    tty: true
    command:
      - python
      - manage.py
      - runserver
      - 0.0.0.0:8000


  postgres:
    container_name: postgres
    image: postgres:18-alpine
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./postgres/init:/docker-entrypoint-initdb.d
    # ports:
    #   # - ${WEBSITE_DOMAIN}:5432:5432
    #   - "${PUBLIC_BIND_IP:-127.0.0.1}:5432:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    networks:
      - ngradar_website_network
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"

      - "traefik.tcp.routers.postgres.rule=HostSNI(`*`)"
      - "traefik.tcp.routers.postgres.entrypoints=postgres"
      - "traefik.tcp.routers.postgres.service=postgres"

      # PostgreSQL's internal container port
      - "traefik.tcp.services.postgres.loadbalancer.server.port=5432"

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"
        ]
      interval: 5s
      timeout: 5s
      retries: 5

  # staging_loader:
  #   container_name: staging_loader
  #   build:
  #     context: .
  #     target: load-staging-data
  #   env_file:
  #     - .env
  #   networks:
  #     - ngradar_website_network
  #   depends_on:
  #     - postgres
  #   volumes:
  #     - staging_dump:/dump
  #   profiles:
  #     - tools

#==========================
# kafka related containers:
#==========================

  kafka-exporter: # Scrape all 3 brokers if possible
    image: danielqsj/kafka-exporter:v1.9.0
    container_name: kafka-exporter
    restart: unless-stopped
    profiles: ["kafka"]
    command:
      - "--kafka.server=kafka-node-1:19092"
      - "--topic.exclude=^__.*"
      - "--refresh.metadata=15s"
    # depends_on:
    #   kafka-node-1:
    #     condition: service_healthy
    networks:
      - ngradar_website_network


# A one-shot init service that creates the Kafka topics after the broker is healthy
  kafka-init:
    container_name: kafka-init
    image: confluentinc/cp-kafka:7.5.1
    # depends_on:
    #   kafka-node-1:
    #     condition: service_healthy
    profiles: ["kafka"]
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS}
    networks:
      - ngradar_website_network
    entrypoint:
    - /bin/sh
    - -ec
    - |
      echo "Creating Kafka topics..."

      kafka-topics --bootstrap-server "$KAFKA_BOOTSTRAP_SERVERS" \
        --create --if-not-exists \
        --topic user_input \
        --partitions 3 \
        --replication-factor 3 \
        --config min.insync.replicas=2

      kafka-topics --bootstrap-server "$KAFKA_BOOTSTRAP_SERVERS" \
        --create --if-not-exists \
        --topic GBT_data \
        --partitions 3 \
        --replication-factor 3 \
        --config min.insync.replicas=2

      kafka-topics --bootstrap-server "$KAFKA_BOOTSTRAP_SERVERS" \
        --create --if-not-exists \
        --topic VLBA_notif \
        --partitions 3 \
        --replication-factor 3 \
        --config min.insync.replicas=2

      kafka-topics --bootstrap-server "$KAFKA_BOOTSTRAP_SERVERS" \
        --create --if-not-exists \
        --topic DSOC_notif \
        --partitions 3 \
        --replication-factor 3 \
        --config min.insync.replicas=2

      echo "Kafka topic initialization complete."
    restart: "no"



  kafka-node-1:
    container_name: kafka-node-1
    hostname: ${KAFKA_EXTERNAL_HOST:-kafka-node-1} # default localhost?
    user: "0:0"
    image: apache/kafka:4.1.2
    profiles: ["kafka"]
    # depends_on:
    #   - zookeeper
    ports:
      - "9091:9091"
      - "19092:19092"
      - "19093:19093"
    volumes:
      - ./jmx-exporter:/opt/jmx-exporter:ro
    environment:
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller

      KAFKA_LISTENERS: BROKER://0.0.0.0:19092,CONTROLLER://0.0.0.0:19093,EXTERNAL://0.0.0.0:9091
      KAFKA_ADVERTISED_LISTENERS: BROKER://${KAFKA_EXTERNAL_HOST:-kafka-node-1}:19092,EXTERNAL://${KAFKA_EXTERNAL_HOST:-localhost}:9091

      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: BROKER:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: BROKER

      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: ${KAFKA_CONTROLLER_QUOROM_VOTERS:-1@kafka-node-1:19093,2@kafka-node-2:29093,3@kafka-node-3:39093}

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 2
      KAFKA_DEFAULT_REPLICATION_FACTOR: 3
      KAFKA_MIN_INSYNC_REPLICAS: 2

      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3

      KAFKA_OPTS: >-
        -javaagent:/opt/jmx-exporter/jmx_prometheus_javaagent-1.6.0.jar=7071:/opt/jmx-exporter/kafka.yml
    networks:
      - ngradar_website_network
      # - proxy
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.docker.network=proxy"

    #   - "traefik.tcp.routers.kafka.rule=HostSNI(`*`)"
    #   - "traefik.tcp.routers.kafka.entrypoints=kafka"
    #   - "traefik.tcp.routers.kafka.service=kafka"

    #   # Kafka's internal container port
    #   - "traefik.tcp.services.kafka.loadbalancer.server.port=9092"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "KAFKA_OPTS='' kafka-topics --bootstrap-server ${KAFKA_EXTERNAL_HOST:-kafka-node-1}:19092 --list >/dev/null 2>&1", 
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s
    


  kafka-node-2:
    container_name: kafka-node-2
    hostname: ${KAFKA_EXTERNAL_HOST:-kafka-node-2} # default localhost?
    user: "0:0"
    image: apache/kafka:4.1.2
    profiles: ["kafka"]
    # depends_on:
    #   - zookeeper
    ports:
      - "9092:9092"
      - "29092:29092"
      - "29093:29093"
    volumes:
      - ./jmx-exporter:/opt/jmx-exporter:ro
    environment:
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_NODE_ID: 2
      KAFKA_PROCESS_ROLES: broker,controller

      KAFKA_LISTENERS: BROKER://0.0.0.0:29092,CONTROLLER://0.0.0.0:29093,EXTERNAL://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: BROKER://${KAFKA_EXTERNAL_HOST:-kafka-node-2}:29092,EXTERNAL://${KAFKA_EXTERNAL_HOST:-localhost}:9092

      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: BROKER:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: BROKER

      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: ${KAFKA_CONTROLLER_QUOROM_VOTERS:-1@kafka-node-1:19093,2@kafka-node-2:29093,3@kafka-node-3:39093}

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 2
      KAFKA_DEFAULT_REPLICATION_FACTOR: 3
      KAFKA_MIN_INSYNC_REPLICAS: 2

      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3

      KAFKA_OPTS: >-
        -javaagent:/opt/jmx-exporter/jmx_prometheus_javaagent-1.6.0.jar=7071:/opt/jmx-exporter/kafka.yml
    networks:
      - ngradar_website_network
      # - proxy
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.docker.network=proxy"

    #   - "traefik.tcp.routers.kafka.rule=HostSNI(`*`)"
    #   - "traefik.tcp.routers.kafka.entrypoints=kafka"
    #   - "traefik.tcp.routers.kafka.service=kafka"

    #   # Kafka's internal container port
    #   - "traefik.tcp.services.kafka.loadbalancer.server.port=9092"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "KAFKA_OPTS='' kafka-topics --bootstrap-server ${KAFKA_EXTERNAL_HOST:-kafka-node-2}:9092 --list >/dev/null 2>&1", 
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s
    


  kafka-node-3:
    container_name: kafka-node-3
    hostname: ${KAFKA_EXTERNAL_HOST:-kafka-node-3} # default localhost?
    user: "0:0"
    image: apache/kafka:4.1.2
    profiles: ["kafka"]
    # depends_on:
    #   - zookeeper
    ports:
      - "9093:9093"
      - "39092:39092"
      - "39093:39093"
    volumes:
      - ./jmx-exporter:/opt/jmx-exporter:ro
    environment:
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_NODE_ID: 3
      KAFKA_PROCESS_ROLES: broker,controller

      KAFKA_LISTENERS: BROKER://0.0.0.0:39092,CONTROLLER://0.0.0.0:39093,EXTERNAL://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: BROKER://${KAFKA_EXTERNAL_HOST:-kafka-node-3}:39092,EXTERNAL://${KAFKA_EXTERNAL_HOST:-localhost}:9093

      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: BROKER:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: BROKER

      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: ${KAFKA_CONTROLLER_QUOROM_VOTERS:-1@kafka-node-1:19093,2@kafka-node-2:29093,3@kafka-node-3:39093}

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 3
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 2
      KAFKA_DEFAULT_REPLICATION_FACTOR: 3
      KAFKA_MIN_INSYNC_REPLICAS: 2

      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3

      KAFKA_OPTS: >-
        -javaagent:/opt/jmx-exporter/jmx_prometheus_javaagent-1.6.0.jar=7071:/opt/jmx-exporter/kafka.yml
    networks:
      - ngradar_website_network
      # - proxy
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.docker.network=proxy"

    #   - "traefik.tcp.routers.kafka.rule=HostSNI(`*`)"
    #   - "traefik.tcp.routers.kafka.entrypoints=kafka"
    #   - "traefik.tcp.routers.kafka.service=kafka"

    #   # Kafka's internal container port
    #   - "traefik.tcp.services.kafka.loadbalancer.server.port=9092"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "KAFKA_OPTS='' kafka-topics --bootstrap-server ${KAFKA_EXTERNAL_HOST:-kafka-node-3}:9093 --list >/dev/null 2>&1", 
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s
    


  kafka-ui:
    container_name: kafka-ui
    image: provectuslabs/kafka-ui:latest
    profiles: ["kafka"]
    ports:
      - "8082:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: multi-node-cluster
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: ${KAFKA_BOOTSTRAP_SERVERS}
    depends_on:
      - kafka-node-1
      # - kafka-broker-2
      # - kafka-broker-3
    networks:
      - ngradar_website_network



#==========================
# sim containers:
#==========================

  gbt:
    container_name: gbt
    build:
      context: .
      target: base
    env_file:
      - .env
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    #   kafka-broker:
    #     condition: service_healthy
    #   kafka-init:
    #     condition: service_completed_successfully
    volumes:
      - .:/service
    networks:
      - ngradar_website_network
    profiles: ["kafka"]
    environment:
      BOOTSTRAP_SERVER: ${KAFKA_BOOTSTRAP_SERVERS} # needs all 3 brokers
    entrypoint:
        - python
        - manage.py
        - gbt_sim



  dsoc:
    container_name: dsoc
    build:
      context: .
      target: base
    env_file:
      - .env
    depends_on:
      dsoc-volume-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      # kafka-node-1:
      #   condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
      seaweedfs:
        condition: service_started
    volumes:
      - .:/service
      - dsoc_transfer:/dsoc/incoming 
    networks:
      - ngradar_website_network
    profiles: ["kafka"]
    environment:
      BOOTSTRAP_SERVER: ${KAFKA_BOOTSTRAP_SERVERS} #needs all 3 brokers 
    entrypoint:
        - python
        - manage.py
        - dsoc_sim
  

  

  vlba:
    container_name: vlba
    build:
      context: .
      target: etc
    env_file:
      - .env
    # depends_on:
      # postgres:
      #   condition: service_healthy
      # kafka-broker:
      #   condition: service_healthy
      # kafka-init:
      #   condition: service_completed_successfully
      # etr_daemon:
      #   condition: service_started
    volumes:
      - .:/service
      - vlba_data:/raw_data
    networks:
      - ngradar_website_network
    profiles: ["kafka"]
    environment:
      BOOTSTRAP_SERVER: ${KAFKA_BOOTSTRAP_SERVERS} # eeds all 3 brokers bootstrap server
      ETD_HOST: ${ETD_HOST}
      ETD_COMMAND_PORT: ${ETD_COMMAND_PORT:-4004}
    entrypoint:
      - python
      - manage.py
      - vlba_sim



#==========================
# storage containers:
#==========================
  seaweedfs:
    container_name: seaweedfs
    build: 
      context: .
      target: seaweedfs
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL}
      WEED_S3_ACCESS_KEY: ${WEED_S3_ACCESS_KEY}
      WEED_S3_SECRET_KEY: ${WEED_S3_SECRET_KEY}
    # ports:
    #   - "8333:8333" # SeaweedFS S3 gateway (ngrok exposes this during demos for our cloud-hosted website to retrieve images)
    #   - "8888:8888"
    #   - "9333:9333"
    volumes:
      - seaweed_data:/data
    depends_on:
      - postgres
    networks:
      - ngradar_website_network
    profiles: ["kafka"]


# Commenting this out because we never use this, but keeping it in case we want to? 
# You can also visit the filer port of the seaweedfs container and view the filer there too.
  # storage-ui:
  #   image: mastertinner/s3manager:latest
  #   platform: linux/amd64
  #   ports:
  #     - "${GUI_PORT:-3000}:8080" 
  #   environment:
  #     - ACCESS_KEY_ID=${WEED_S3_ACCESS_KEY:-anykey}
  #     - SECRET_ACCESS_KEY=${WEED_S3_SECRET_KEY:-anysecret}
  #     - ENDPOINT=seaweedfs:8333 
  #     - USE_SSL=false
  #   networks:
  #     - ngradar_website_network


#==========================
# etransfer containers:
#==========================
# a one-shot init service that creates the volume before etd starts
  dsoc-volume-init:
    container_name: dsoc-volume-init
    image: alpine:3.20
    profiles: ["kafka"]
    user: "0:0"
    volumes:
      - dsoc_transfer:/dsoc/incoming
    command:
      - /bin/sh
      - -c
      - |
        mkdir -p /dsoc/incoming
        chown -R 33:33 /dsoc/incoming
        chmod 775 /dsoc/incoming

  etr_daemon:
    container_name: etr_daemon
    build:
      context: .
      target: etd
    profiles: ["kafka"]
    user: "33:33"   # non root user: www-data
    # ports:
    #   - "${PUBLIC_BIND_IP:-127.0.0.1}:4004:4004/tcp"
    #   - "${PUBLIC_BIND_IP:-127.0.0.1}:8008:8008/udp"
    depends_on:
      dsoc-volume-init:
        condition: service_completed_successfully
    networks:
      - ngradar_website_network
      - proxy
    volumes:
      - dsoc_transfer:/dsoc/incoming
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"

      # TCP traffic on port 4004
      - "traefik.tcp.routers.etransfer-command.rule=HostSNI(`*`)"
      - "traefik.tcp.routers.etransfer-command.entrypoints=etransfer-command"
      - "traefik.tcp.routers.etransfer-command.service=etransfer-command"
      - "traefik.tcp.services.etransfer-command.loadbalancer.server.port=4004"

      # UDP/SRT traffic on port 8008
      - "traefik.udp.routers.etransfer-data.entrypoints=etransfer-data"
      - "traefik.udp.routers.etransfer-data.service=etransfer-data"
      - "traefik.udp.services.etransfer-data.loadbalancer.server.port=8008"
    command:
      - etd
      - -f
      - --command
      - tcp://:4004
      - --data
      - srt://:8008



#==========================
# end of containers
#==========================


volumes:
  postgres_data:
  staging_dump:
  seaweed_data:
  dsoc_transfer:
  vlba_data:
  portainer_data:
  prometheus_data:
  grafana_data:
  zookeeper-data:
  zookeeper-log:
  kafka-1-data:
  kafka-2-data:
  kafka-3-data:

networks:
  ngradar_website_network:
    driver: bridge
  proxy:
    name: proxy
    driver: bridge